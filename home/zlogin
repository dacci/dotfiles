# .zlogin

# ssh-agent
#[ -z "$SSH_AUTH_SOCK" ] && eval `ssh-agent`
#[ -n "$SSH_AUTH_SOCK" ] && ssh-add

# Launch screen
#case "$TERM" in
#  screen*)
#    ;;
#
#  *)
#    if tty -s; then
#      if tmux has-session; then
#        tmux attach-session
#      else
#        tmux
#      fi
#    fi
#    ;;
#esac
